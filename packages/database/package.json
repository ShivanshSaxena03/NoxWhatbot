{
  "name": "@jarvis/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "build:local": "dotenv -e ../../.env -- prisma generate && tsc",
    "db:generate:local": "dotenv -e ../../.env -- prisma generate",
    "db:push:local": "dotenv -e ../../.env -- prisma db push",
    "db:migrate:local": "dotenv -e ../../.env -- prisma migrate dev"
  },
  "dependencies": {
    "@jarvis/security": "1.0.0",
    "@jarvis/shared": "1.0.0",
    "@prisma/client": "^5.10.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prisma": "^5.10.2",
    "typescript": "^5.3.3"
  }
}
